# Generated/build output — only twa-manifest.json, .gitignore and README.md
# are the source of truth; everything else is regenerated by `bubblewrap update`.
.gradle/
build/
app/
gradle/
gradlew
gradlew.bat
build.gradle
settings.gradle
gradle.properties
manifest-checksum.txt
store_icon.png
*.apk
*.aab
*.idsig

# NEVER commit signing material.
*.keystore
*.jks
local.properties
